# 경남수학문화관 SW체험 타이머 — 인수인계 문서

작성: 이상우 · 경남수학문화관
용도: 세로형 키오스크(디지털 사이니지)에서 체험 시간 안내용 타이머

---

## 1. 구성 파일

| 파일 | 역할 | 비고 |
|---|---|---|
| `timer.html` | 타이머 본체 (웹앱) | 단독 실행 가능. 로고 이미지가 파일 안에 내장되어 별도 이미지 파일 불필요 |
| `launcher.ps1` | 업데이트 확인 + Edge 전체화면 실행 | GitHub `main` 의 `version.txt` 와 비교해 다르면 최신 파일로 교체 |
| `타이머시작.bat` | 더블클릭/부팅 시 실행 진입점 | `launcher.ps1` 을 호출만 함 |
| `version.txt` | 설치된 버전 | 배포 시 이 값을 바꾸면 키오스크가 업데이트를 받음 |
| `설치안내.txt` | 설치·자동실행 절차 | 참고용 |

핵심 규칙: **모든 파일은 반드시 같은 폴더에 둔다.** (예: `C:\timer\`)
bat 는 "자기와 같은 폴더의 launcher.ps1" 을, launcher 는 "같은 폴더의 timer.html" 을 여는 구조라 폴더가 다르면 오류가 난다.
시작프로그램 폴더에는 bat 의 **바로 가기**만 넣는다.

소스는 GitHub 저장소 `i20091119-ai/2026.time` 에 있다.

---

## 2. 실행 환경

- 대상: Windows 10/11 + Microsoft Edge (키오스크 모드). PowerShell 5.1 이상(기본 내장)
- 해상도: 세로형 9:16 기준 (1080×1920 등). vw/vh 기반이라 해상도가 달라도 잘리지 않음.
- 키오스크 구조상 **아래 1/3 은 책상에 가려 보이지 않는다.** 그래서 화면을 세로로 3등분해
  상단 1/3 = 현재 시각, 중간 1/3 = 타이머, 하단 1/3 = 공백 으로 배치했다.

---

## 3. 기능 요약

- 상단: 로고, 오늘 날짜(요일 포함), 현재 시각 `HH:MM` + 작은 초
- 중간: 프리셋 **5분 / 20분 / 25분** (기본 20분), **시작 ↔ 일시정지**(토글), **초기화**
- 원형 진행 표시. 남은 1분 이하부터 숫자·링이 빨간색으로 전환
- **종료 30초 전부터 매초 "삐빅"**, 마지막 5초는 더 높고 강한 음
- **종료 시** 화면 깜빡임 + 길게 "삐— 삐—"
- 좌상단 ⛶ 전체화면 토글 / 우상단 ▁ 최소화(중간 영역에 남은 시간 숫자만 크게 표시)
- 페이지 로드 시 또는 첫 화면 터치 시 자동 전체화면 진입
- 키보드 안내 줄 끝에 현재 버전 표시 (`v2026.09.22.1` 등)

### 키보드 단축키
| 키 | 동작 |
|---|---|
| 1 | 5분 설정 |
| 2 | 20분 설정 |
| 3 | 25분 설정 |
| 4 | 초기화 |
| Enter (또는 Space) | 시작 / 일시정지 |

무선 프리젠터(리모컨)의 화면 전환 버튼도 대개 Enter/Space로 잡혀 시작·정지가 됨.

주의: **소리는 사용자가 한 번 조작(시작 버튼 등)한 뒤부터** 재생됨(브라우저 자동재생 정책). 키오스크에선 시작 버튼을 누르므로 정상 작동. 부팅 후 소리가 안 나면 Windows 볼륨/음소거부터 확인.

---

## 4. 자동 실행 설정 (부팅 시 자동으로 타이머 뜨게)

1. 저장소 ZIP 을 받아 압축을 풀고 폴더째 `C:\timer\` 에 둔다
2. `Win + R` → `shell:startup` 입력 → 엔터 → "시작프로그램" 폴더가 열림
3. 그 폴더에 `타이머시작.bat` 의 **바로 가기** 를 넣는다
4. (선택) 로그인 화면까지 건너뛰려면: `Win + R` → `netplwiz` → "사용자 이름과 암호를 입력해야…" 체크 해제 → 계정 암호 입력

종료 방법: `Ctrl + Shift + W` 또는 `Alt + F4`

---

## 5. 자동 업데이트

`launcher.ps1` 이 실행될 때마다:

1. GitHub `main` 브랜치의 `version.txt` 를 읽는다 (8초 안에 응답 없으면 건너뜀)
2. 로컬 `version.txt` 와 다르면 `timer.html`, `launcher.ps1`, `설치안내.txt` 를 임시 폴더에 받는다
3. 모두 정상이면 교체하고 `version.txt` 갱신. 하나라도 실패하면 기존 파일 유지
4. Edge 키오스크로 실행

기록: 같은 폴더 `update.log`

### 새 버전 배포 절차
1. `timer.html` 수정
2. `version.txt` 값 변경 (예: `2026.09.23.1`)
3. GitHub `main` 에 반영
4. 키오스크 재부팅(또는 bat 재실행) 시 자동 적용

### 업데이트를 끄고 싶을 때
`launcher.ps1` 의 `$RepoRaw` 줄을 주석(`#`) 처리하면 확인 자체를 건너뛰고 바로 실행한다.
(또는 인터넷을 끊어도 동일하게 동작)

---

## 6. 자주 하는 수정

내용을 고쳐도 **파일명(`timer.html`)과 위치가 그대로면 bat/launcher 는 손댈 필요 없다.**
아래는 `timer.html` 을 메모장/편집기로 열어 고치는 위치.

### 프리셋 시간 바꾸기
`<div class="presets">` 안의 버튼에서 `data-min` 값과 글자를 바꾼다.
```html
<button class="preset" data-min="5">5분</button>
<button class="preset active" data-min="20">20분</button>
<button class="preset" data-min="25">25분</button>
```
`active` 가 붙은 버튼이 시작 시 기본 선택. 버튼을 추가/삭제하면 아래 키보드 단축키(`keydown`)와 안내 줄(`keyguide`)도 같이 맞춘다.

### 삐빅 시작 시점(30초) 바꾸기
`tick()` 함수 안:
```javascript
if(secLeft<=30 && secLeft!==lastBeepSec){   // 이 30 을 원하는 초로
```

### 영역 비율 바꾸기 (예: 하단 가림 영역이 1/4 일 때)
`#app` 의 `grid-template-rows:1fr 1fr 1fr;` 를 원하는 비율로 (예: `1fr 1.25fr .75fr`).

### 현재 시각 초 숨기기
`updateClock()` 에서 `<span class="sec">…</span>` 부분을 지운다.

### 색상 바꾸기
`:root` 의 CSS 변수:
- `--accent` 강조색(노랑) · `--danger` 경고색(빨강) · `--run` 진행색(초록) · `--bg` 배경

### 소리 톤 바꾸기
- 카운트다운음: `shortBeep()` 의 `o.frequency.value`
- 종료음: `beep()` 의 `o.frequency.value`

---

## 7. 코드

전체 코드는 저장소의 `timer.html`, `launcher.ps1`, `타이머시작.bat` 파일 자체를 본다.
(이 문서에 복사해 두면 수정할 때마다 어긋나므로 싣지 않는다.)
